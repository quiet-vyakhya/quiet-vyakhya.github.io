title: "Your Blog Title"
description: "Daily reflections on philosophy, poetry, language, and quiet inquiry."
theme: minima

# Ensure full text is included in RSS feeds for offline reading
plugins:
  - jekyll-feed
  - jekyll-seo-tag
